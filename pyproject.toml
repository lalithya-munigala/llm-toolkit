[project]
name = "llm-toolkit"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "lalithya", email = "code.verse330@gmail.com" }
]
requires-python = ">=3.11"
dependencies = []

[project.scripts]
llm-toolkit = "llm_toolkit:main"

[build-system]
requires = ["uv_build>=0.12.14,<0.13.0"]
build-backend = "uv_build"
